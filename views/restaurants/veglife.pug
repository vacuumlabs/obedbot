table.table.table-sm
  thead.thead-light
    tr
      - var i = 1;
      while i <= totalMeals
        th= i++
  tbody
    tr
      - var i = 1;
      while i <= totalMeals
        td #{(main && main[i]) || ''}
        - i++

table.table.table-sm
  thead.thead-light
    tr
      th='Polievky'
      th='Šaláty'
  tbody
    tr
      td #{(soup && soup.p) || 0}
      td #{(salad && salad.s) || 0}
